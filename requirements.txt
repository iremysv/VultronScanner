# =============================================================================
# VultronScanner — Dependency Manifest
# Attack Surface Manager | İstinye Üniversitesi Final Projesi
# =============================================================================

# ── Core Async Runtime ────────────────────────────────────────────────────────
aiohttp==3.9.5          # Async HTTP client — CVE/NVD API sorguları & web analizi
aiofiles==23.2.1        # Async dosya I/O — rapor yazımı
httpx==0.27.0           # Modern async HTTP client — WebAnalyzer & VulnerabilityEngine

# ── Network & Discovery ───────────────────────────────────────────────────────
python-nmap==0.7.1      # Nmap Python wrapper — Discovery Engine core'u
dnspython==2.6.1        # DNS çözümleme — HostDiscovery & NetworkUtils
ipwhois==1.3.0          # Whois & ASN sorguları — hedef istihbaratı
ipaddress               # CIDR/IP validasyon (stdlib, explicit listing)

# ── Data Validation & Configuration ──────────────────────────────────────────
pydantic==2.7.1         # Tip güvenli veri modelleri — scan result schemas
PyYAML==6.0.1           # YAML config dosyası okuma — Config katmanı

# ── CLI Interface ─────────────────────────────────────────────────────────────
click==8.1.7            # Profesyonel CLI framework — main.py giriş noktası
rich==13.7.1            # Renkli terminal çıktısı — Logger & progress bars

# ── Report Generation ─────────────────────────────────────────────────────────
Jinja2==3.1.4           # HTML rapor şablonu render motoru
markdown2==2.4.13       # Markdown → HTML dönüşümü

# ── Security & Cryptography ───────────────────────────────────────────────────
cryptography==42.0.7    # Sertifika analizi için

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0           # Test framework
pytest-asyncio==0.23.6  # Async test desteği
pytest-cov==5.0.0       # Test coverage raporu

# ── Development Utilities ─────────────────────────────────────────────────────
python-dotenv==1.0.1    # .env dosyasından API key yükleme
requests==2.32.2        # Senkron HTTP (fallback & CVE API)
